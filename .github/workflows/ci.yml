name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  install:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install Poetry
        run: pipx install poetry

      - name: Install dependencies
        run: poetry install --no-root

      - name: Collect data
        run: poetry run python ./src/data/collect/collect.py

      - name: Verify outputs
        run: |
          for season in 2015-16 2016-17 2017-18 2018-19 2019-20 2020-21 2021-22 2022-23 2023-24 2024-25; do
            test -f "data/matches/with_xg/${season}.csv"
            test -f "data/matches/with_xg_and_elo/${season}.csv"
          done